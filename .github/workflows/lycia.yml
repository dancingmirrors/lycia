name: lycia

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: actions/checkout@v6
        uses: actions/checkout@v6

      - name: make
        run: |
          set -x
          make
          sudo make install
          sudo lycia
          sudo pkill lycia
